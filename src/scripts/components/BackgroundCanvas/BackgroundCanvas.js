/*import React, { Component } from 'react';import './BackgroundCanvas.css';const bgOptions = {    space: {        url: 'https://github.com/straker/galaxian-canvas-game/blob/master/part1/imgs/bg.png?raw=true',        width: 600,        height: 360    }};class Canvas extends Component {    constructor() {        super();        this.x = 0;        this.y = 0;        this.speed = 1;        this.canvasWidth = 0;        this.canvasHeight = 0;        this.context;        this.draw = this.draw.bind(this);        this.imageElement = new Image();        this.state = {            selectedOption: 'space'        };    }    set canvasContext(canvasElement) {        this.context = canvasElement.getContext('2d');        this.canvasWidth = canvasElement.width;        this.canvasHeight = canvasElement.height;    }    get backgroundImg() {        this.imageElement.src = bgOptions[this.state.selectedOption] && bgOptions[this.state.selectedOption].url;        return this.imageElement;    }    get selectedBackground() {        return bgOptions[this.state.selectedOption];    }    componentDidMount() {        this.canvasContext = this.canvasElement;        this.animate();    }    animate() {        window.setInterval(this.draw, 15);    }    draw() {        this.y += this.speed;        this.context.drawImage(this.backgroundImg, this.x, this.y);        this.context.drawImage(this.backgroundImg, this.x, this.y - this.canvasHeight);        if (this.y >= this.canvasHeight) this.y = 0;    }    render() {        return (            React.createElement("div", null,                React.createElement("canvas", {                    id: "background",                    ref: canvas => this.canvasElement = canvas,                    width: this.selectedBackground.width,                    height: this.selectedBackground.height                })));    }}export default Canvas;*/